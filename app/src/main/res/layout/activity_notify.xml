<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              xmlns:notification="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:background="@android:color/white"
              tools:context=".NotificationAlertActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="58dp"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:titleTextColor="#ffffff">

        <com.animationlib.notification.NotificationAnimView
            android:id="@+id/custom_animview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            notification:image_width = "100dp"
            notification:image_height = "100dp"
            notification:image_padding = "5dp"
            notification:textview_width = "25dp"
            notification:textview_height = "25dp"
            notification:textview_textsize = "5sp"
            notification:textview_margin = "20dp"
            />

    </android.support.v7.widget.Toolbar>

    <Button
        android:id="@+id/btn_notify"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="10dp"
        android:text="New Notification"/>

</LinearLayout>